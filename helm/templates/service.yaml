apiVersion: v1
kind: Service

metadata:
  name: payment-system

spec:

  selector:
    app: payment-system

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080

  type: {{ .Values.service.type }}